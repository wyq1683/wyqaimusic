; Wave 音乐播放器 — PHP 运行安全加固（InfinityFree 每目录生效）
; 隐藏 PHP 版本号，避免攻击者按版本定向利用
expose_php = Off

; 关闭错误回显，防止 SQL/路径/堆栈信息泄露给前端
display_errors = Off
display_startup_errors = Off
log_errors = On

; 会话 Cookie 安全属性
session.cookie_httponly = 1
session.cookie_secure = 1
session.cookie_samesite = Lax
